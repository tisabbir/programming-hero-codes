const a = 6;
const b = 6;
const person = {
    name : "jhakar",
    age : 43,
}

const age = [43,45,56,75]
console.log(`Hello world?
the sum of ${a} and ${b} is ${a+b}. said by ${person.name} during is  ${age[2]}th Birthday`);
