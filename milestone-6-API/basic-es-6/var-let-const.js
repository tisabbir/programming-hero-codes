/**
 * Var --> We should never use it 
 * let --> when the variable needs to be changed (10% cases)
 * const --> when the variable don't needs to be changed (90% cases)
 * */ 

/**
 * The array and the objects can be modified when declared as const 
 * But they cannot be reassigned when used as an array
 * 
 */